{
  "permissions": {
    "allow": [
      "Bash(claude plugin validate *)",
      "Bash(./scripts/validate.sh)",
      "Bash(git status *)",
      "Bash(git diff *)",
      "Bash(git log *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *skills/*/SKILL.md|*.claude-plugin/*.json) \"$CLAUDE_PROJECT_DIR\"/scripts/validate.sh >&2 || echo \"validation failed after editing $f\" >&2 ;; esac; exit 0"
          }
        ]
      }
    ]
  }
}
